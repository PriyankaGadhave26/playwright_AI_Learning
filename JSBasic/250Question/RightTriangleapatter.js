n=5
for(let i=1;i<=n;i++) // to controls number of rows
{
    let row='';
    for( let j=1;j<=i;j++)//builds stars for that row
    {
        row += '*' //We store stars in
    }
   console.log(row)
}